@model IEnumerable<WebApplicationMD4.ModelsDB.Course>

@{
    ViewData["Title"] = "Index";
}

<h1>Courses List</h1>

<p>
    <a class="button create_button" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teacher)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.Fullname)
            </td>
            <td>
                <a class="button edit_button" asp-action="Edit" asp-route-id="@item.CourseId">Edit</a> |
                <a class="button details_button" asp-action="Details" asp-route-id="@item.CourseId">Details</a> |
                <a class="button delete_button" asp-action="Delete" asp-route-id="@item.CourseId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
